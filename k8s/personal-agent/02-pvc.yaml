apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-data
  namespace: personal-agent
  labels:
    app.kubernetes.io/part-of: personal-agent
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
