apiVersion: batch/v1
kind: CronJob
metadata:
  name: digest-morning
  namespace: personal-agent
  labels:
    app: digest-morning
    app.kubernetes.io/part-of: personal-agent
spec:
  schedule: "0 9 * * *"
  timeZone: "Europe/Lisbon"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          nodeSelector:
            personal-agent: "true"
          containers:
            - name: digest-morning
              image: josecarlosjr/app-cla-agent:sha-726e968
              command: ["python", "digest.py", "morning"]
              envFrom:
                - secretRef:
                    name: agent-secrets
              env:
                - name: DATA_DIR
                  value: /data
              volumeMounts:
                - name: agent-data
                  mountPath: /data
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 300m
                  memory: 256Mi
          volumes:
            - name: agent-data
              persistentVolumeClaim:
                claimName: agent-data
          restartPolicy: OnFailure
