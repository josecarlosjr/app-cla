apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: personal-agent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: postgres-local
  resources:
    requests:
      storage: 10Gi
